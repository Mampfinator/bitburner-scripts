const { React } = globalThis;

export interface DashboardProps {

}

/**
 * Dashboard for everything happening in the game. Features should include, but not be limited to:
 * - [] Server buying
 * - [] Memory inspection (theoretical vs. actual)
 * - [] Hack inspection, overrides, ...
 * - [] Gang inspection, overrides, ...
 * - [] Corp inspection
 */
export function Dashboard(props: DashboardProps) {
    return <div>MASSIVE TODO</div>
}
